# Run records

Use `code/run_logged.sh` to capture ignored raw logs under `logs/runs/` and
compact Markdown records here. Records may be committed when they document an
important production audit, pilot, batch, retry, or validation result without
including private trial-level data.
